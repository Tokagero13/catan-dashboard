# Catan Manager

Веб-приложение для управления играми в Catan с отслеживанием статистики игроков, групп и турниров.

## Возможности

- 🎮 Генератор карт Catan
- 👥 Управление группами игроков
- 📊 Система лидерборда
- 🏆 Отслеживание достижений
- 📈 Статистика игр
- 📷 Фото победителей

## Технологии

- **Frontend**: React + TypeScript + Vite
- **Backend**: Node.js + Express + SQLite
- **Стилизация**: Tailwind CSS

## Быстрый старт

### 1. Установка зависимостей

```bash
# Установка frontend зависимостей
npm install

# Установка backend зависимостей
npm run postinstall
```

### 2. Запуск в development режиме

```bash
npm run dev
```

Это запустит:
- Frontend на http://localhost:3000
- Backend на http://localhost:3002

### 3. Production деплой

Для production деплоя с Nginx:

1. **Соберите проект:**
```bash
npm run build:prod
```

2. **Добавьте в Nginx конфигурацию** (см. `nginx-config.txt`):
```nginx
location /api/ {
    rewrite ^/api/(.*)$ /$1 break;
    proxy_pass http://YOUR_SERVER_IP:3002;
    # ... остальные заголовки
}
```

3. **Запустите backend:**
```bash
cd catan-server && node index.js
```

## Структура проекта

```
├── components/          # React компоненты
├── pages/              # Страницы приложения
├── services/           # API сервисы
├── catan-server/       # Backend Node.js
├── src/               # Vite типы и конфигурация
├── .env               # Переменные окружения
└── nginx-config.txt        # Конфигурация Nginx
```

## Конфигурация

### Переменные окружения

- `VITE_API_URL` - URL API сервера (по умолчанию `/api` для production)
- `VITE_ALLOWED_HOSTS` - Разрешенные хосты для Vite dev server

### Backend

Backend автоматически создает SQLite базу данных и необходимые таблицы при первом запуске.

## Команды

```bash
npm run dev              # Запуск development сервера (frontend + backend)
npm run dev:frontend     # Только frontend
npm run dev:backend      # Только backend
npm run build            # Сборка для production
npm run build:prod       # Сборка с production переменными
npm run preview          # Предпросмотр production сборки
```

## API Endpoints

- `GET /groups` - Получить все группы
- `POST /groups` - Создать группу
- `GET /sessions` - Получить все сессии игр
- `POST /sessions` - Создать сессию игры
- `GET /leaderboard` - Получить лидерборд
