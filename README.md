## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Open your browser to `http://localhost:3000`

## Run [Server](catan-server)

1. cd catan-server
2. npm install
3. npm run start

